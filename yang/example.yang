module example {
    namespace "http://aronj.is/example";
    prefix "example";

    typedef hostname {
        type string;
    }

    container interfaces {
        list TenGigE {
            key name;
            leaf name {
                type string;
            }
        }
    }

    container first {
        list second {
            key hostname;
            leaf hostname {
                type hostname;
            }
            container third;
        }
    }
}
